# Copy to `.env` locally, or set in Vercel → Project → Settings → Environment Variables.
# Both require the VITE_ prefix (that's how Vite exposes them to the browser).

# Google Analytics 4 Measurement ID (G-XXXXXXXX)
# Leave empty to disable analytics completely (no script loaded, no network requests)
VITE_GA_ID=
